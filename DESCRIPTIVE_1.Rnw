\documentclass{article}

\begin{document}

<<>>=
df <- read.csv("epa.csv")
nrow(df)
str(df)
@

Display the structure of the dataframe

<<>>=
dfH = df[df$C.H=='H',]
nrow(dfH)
dfT = df[df$car.truck=='T',]
nrow(dfT)
@

<<>>=
Sys.info()[["user"]]
@
\end{document}